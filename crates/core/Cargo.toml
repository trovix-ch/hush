[package]
name = "wl-core"
description = "Platform-neutral types, traits and state machine for whisper-local"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
toml = "0.9"
tracing = "0.1"

[lints]
workspace = true
