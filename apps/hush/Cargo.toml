[package]
name = "hush"
description = "Local-only voice dictation for Windows"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
default = ["llama-cpp"]
# The embedded LLM normalizer; without it cleanup is rules-only or over HTTP.
llama-cpp = ["hush-normalize/llama-cpp"]
# Speech inside this process (`engine.in_process = true`) instead of hush-stt-worker.exe,
# for debugging. transcribe.cpp then comes as DLLs next to hush.exe so its ggml cannot
# collide with llama.cpp's; built together with the worker, the worker uses them too.
in-process-stt = ["dep:hush-stt-worker", "hush-stt-worker/shared"]

[dependencies]
hush-core = { path = "../../crates/core" }
hush-audio = { path = "../../crates/audio" }
hush-stt = { path = "../../crates/stt" }
hush-stt-worker = { path = "../hush-stt-worker", optional = true }
hush-normalize = { path = "../../crates/normalize", default-features = false }
hush-platform-windows ={ path = "../../crates/platform-windows" }
anyhow = "1"
ctrlc = "3"
hound = "3.5"
serde_json = "1"
tracing = "0.1"
tracing-appender = "0.2"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }
# No TLS: the only server this talks to is the user's own local inference server.
ureq = { version = "3", default-features = false }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_System_Console",
    "Win32_System_Registry",
    "Win32_System_StationsAndDesktops",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }

[lints]
workspace = true
