[package]
name = "typing"
version = "0.0.0"
edition = "2024"
publish = false

# Standalone: this spike is deliberately outside the main workspace.
[workspace]

[dependencies]
windows = { version = "0.62", features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_System_LibraryLoader",
    "Win32_System_Threading",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Input_KeyboardAndMouse",
] }
